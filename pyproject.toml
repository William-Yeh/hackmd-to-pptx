[project]
name = "hackmd-to-pptx"
version = "1.3.0"
requires-python = ">=3.9"
dependencies = [
    "python-pptx>=0.6.21",
    "lxml>=4.9.0",
    "PyYAML>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
